# Pinned to the competition inference environment (python 3.12.12).
# CUDA wheels come from the pytorch/pytorch:2.12.0-cuda12.6 base image at build time.
torch==2.12.0
torchvision==0.27.0
timm==1.0.27
albumentations==2.0.8
omegaconf==2.3.0
pandas==3.0.3
numpy==2.4.6
pillow==12.2.0
